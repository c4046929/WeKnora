ALTER TABLE evaluation_model_calls
    ADD COLUMN IF NOT EXISTS pricing_configured BOOLEAN NOT NULL DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS currency VARCHAR(16) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS input_price_per_million DOUBLE PRECISION NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS output_price_per_million DOUBLE PRECISION NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS cache_read_price_per_million DOUBLE PRECISION NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS cache_write_price_per_million DOUBLE PRECISION NOT NULL DEFAULT 0,
    ADD COLUMN IF NOT EXISTS estimated_cost DOUBLE PRECISION NOT NULL DEFAULT 0;
